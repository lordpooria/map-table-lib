import { PlayerCompleteProps } from "./types";
declare const PlayerControl: ({ leafletMap, buffer, loop, minBufferReady, timeSteps, autoPlay, startedOver, minSpeed, maxSpeed, loadingTimeout, }: PlayerCompleteProps) => JSX.Element;
export default PlayerControl;
