## Player Props Remove Component

You could remove player component or icons

import {PlayerAddRemoveComponents} from "../../components/PlayerProps";

<PlayerAddRemoveComponents />

import SyntaxHighlighter from "../../../SyntaxHighlighter";

<SyntaxHighlighter>
  {`import React from "react";
import HesabaTimeDimension from "@hesaba/map";
import data from "../data/data.json";
export default function BaseMapScreen() {
 return(
  return (
    <HesabaTimeDimension
      mapProps={{
        center: [35.76498031616211, 51.33673858642578],
        zoom: 13,
        zoomControl: false,
        className: classes.container
      }}
      data={data}
      playerProps={{
        speedIcon: false,
        forwardButton: false,
        speedSlider: false,
      }}
    />
  );
 )
}
`}
</SyntaxHighlighter>
;