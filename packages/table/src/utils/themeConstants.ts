export const toolbar = {
  xs: 30,
  minHeight: 44,
  maxHeight: 88,
};
export const sidebar = {
  maxWidth: 300,
  minWidth: 30,
};

export const PanelIconSize = 18;

export const maxPanelSize = 250;

export const commonSidebar = {
  panelHeight: 200,
  itemHeight: 50,
};
export const DIALOG_CONTENT_HEIGHT =
  typeof window !== "undefined" ? window.innerHeight * 0.8 : 800;

export const MIN_TABLE_WIDTH = 24;
export const RESIZE_HANDLE_WIDTH = 24;

export const ROW_MIN_WIDTH = 100
export const CHECKBOX_SIZE = 48
