import { HeaderCellProps } from "../types/tableElements";
declare const HeadCell: ({ minWidth, label, colKey, CellComponent, HeaderComponent, visible, sorted, sortable, resizable, colIndex, classes, sticky, DividerProps, ...rest }: HeaderCellProps) => JSX.Element;
export default HeadCell;
