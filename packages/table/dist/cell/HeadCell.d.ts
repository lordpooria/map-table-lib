import { HeaderCellProps } from "@/types/tableElements";
declare const HeadCell: ({ minWidth, label, colKey, CellComponent, HeaderComponent, visible, sorted, sortable, resizable, colIndex, currentWidths, classes, ...rest }: HeaderCellProps) => JSX.Element;
export default HeadCell;
