import { TimeProps } from "./TimeComponent.types";
declare const TimerComponent: ({ am, pm, noTimeError, clockProps, dateWrapperClasses, dateClasses, amPmClasses, }: TimeProps) => JSX.Element;
export default TimerComponent;
