import { PlayerCompleteProps } from "./PlayerControl.types";
declare const PlayerControl: ({ leafletMap, loop, timeSteps, autoPlay, startedOver, minSpeed, maxSpeed, classes, transitionTime, backwardButton, forwardButton, playButton, timeSlider, speedSlider, speedIcon, playReverseButton, }: PlayerCompleteProps) => JSX.Element;
export default PlayerControl;
