import { PlayerCompleteProps } from "../types/PlayerControl";
declare const PlayerControl: ({ leafletMap, loop, timeSteps, speedStep, autoPlay, startedOver, minSpeed, maxSpeed, classes, transitionTime, backwardButton, forwardButton, playButton, timeSlider, speedSlider, speedIcon, playReverseButton, }: PlayerCompleteProps) => JSX.Element;
export default PlayerControl;
